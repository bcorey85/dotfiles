## Critical Rules
     
- NEVER commit, push, or otherwise modify git history without explicit user approval. Wait for the user to say "commit" or "push" before doing so.
- NEVER run destructive commands without prompting the user first.
- If you're unsure whether an action needs approval, ask.
