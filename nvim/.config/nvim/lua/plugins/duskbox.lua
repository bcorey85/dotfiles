-- duskbox — ih-hugh/duskbox, an OKLCH-generated equiluminant family. dusk
-- (indigo night) dark / dawn (warm paper) light. Unlike the older Vimscript
-- families, it ships full treesitter @-captures, Diagnostic*, and @lsp.type
-- semantic tokens out of the box, so theme-sync's FAMILIES entry carries only
-- accents — no comment-floor or diff fixup. lazy.nvim auto-loads it on
-- :colorscheme duskbox-dusk|dawn (registered by colors/duskbox-*.lua).
return { "ih-hugh/duskbox", lazy = true }
