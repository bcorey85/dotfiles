---
description: Produce a durable decision record (ADR) from the eng-spec task dir and branch diff — run before the PR opens, so it ships in the same PR as the code
---

Load the `adr` skill and follow its instructions. $ARGUMENTS
