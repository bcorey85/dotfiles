# Rule anchors

Every heading block of the prompt files listed below has a content hash and the
reason it exists. `claude-doctor` recomputes the hashes and reports two faults: a
block with no row, and a row whose block no longer exists. Whitespace does not move
a hash.

The prompt files never carry this provenance. It lives here, and the change ledger
(`agent-evals.md`) holds the full story behind each evidence kind.

To change an anchored block:

1. Edit the prompt file.
2. Run `~/.claude/scripts/rule-anchors.sh list <file>` and copy the new hash.
3. Replace the row's hash. Set `evidence` and `reason` to what backs the new text.
4. Run `claude-doctor`. Git history keeps the old rows.

Evidence kinds: `gate ranking`, `census`, `removal round`, `seeded round`,
`controlled replay`, `judgment` (nobody measured it), `unproven` (never exercised).

| hash     | file                                   | section                                                     | evidence                    | reason                                                                                                                                                                               |
| -------- | -------------------------------------- | ----------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 6b04ae9b | agents/code-reviewer.md                | (preamble)                                                  | seeded round, removal round | Dispatch contract: name, description, tools, project memory, and the one-line role. The opus pin stays because sonnet at this gate missed planted defects that the same prompt caught on opus. The role line's restraint clause went with the judgment text: two replicates at opus found the reviewer without it equal on recall and on false findings. |
| b4ab6a17 | agents/code-reviewer.md                | ## Persistent Memory                                        | unproven                    | Project memory of known patterns, copied from the shared calibration file when the diff reviewer stopped loading that file. Never exercised: every review round dispatches without a memory directory. |
| fbc646b2 | agents/code-reviewer.md                | ## Specialist Scope                                         | removal round               | The four specialist-ownership bullets, kept verbatim in the contract-only text that matched the full prompt in two replicates at opus. Not themselves varied. The suppression list they sat in is gone: inert on recall and on false findings. |
| 9411a7fc | agents/code-reviewer.md                | ### Step 1: Determine Scope                                 | removal round               | How the reviewer finds the diff: the handoff block first, else the git working state. Kept verbatim in the contract-only text that matched the full prompt in two replicates at opus. Not itself varied. |
| 8aac1ba9 | agents/code-reviewer.md                | ### Step 2: Read the Changes                                | removal round               | Read every file in scope and the project conventions file. The three required audits and the two guard sentences that followed them are gone: two replicates at opus found the reviewer without them equal on recall and on false findings, at about seven tenths of the tokens. |
| 8b91a12b | agents/code-reviewer.md                | ## Output Format                                            | removal round, judgment     | The report template the dispatcher parses: summary headings, prior issues, Fix/Ask/Nit. Kept verbatim in the contract-only text that matched the full prompt in two replicates at opus, plus one sentence added by judgment: the plan-impact prefix that the review loop scans for. That prefix used to live in the removed disposition text, and no bench exercises it. |
| 1abfce31 | agents/code-reviewer.md                | ## Reviewer-Specific Tool Use                               | removal round               | What remains after the removal: trust the handoff's checks, and stay in scope with two standing exceptions. Kept in the contract-only text that matched the full prompt in two replicates at opus. |
| 31c1975f | agents/test-writer.md                  | (preamble)                                                  | removal round               | Dispatch contract: name, description, the sonnet pin, no Agent tool, and the two-line role: one phase, after the implementation, without reading it. Kept verbatim in the contract-only text that matched the full prompt on planted-bug kills and on tokens in eight Sonnet runs per arm. The sonnet pin is untested. |
| 42459b9d | agents/test-writer.md                  | ## Implementation blindness (HARD RULE — the reason you exist) | removal round, census    | The MAY and MUST NOT lists, the plan-gap rule, and the one-sentence vocabulary rule. Kept in the contract-only text that matched the full prompt. The "stop scrolling" sentence and the pointer to the vocabulary file are gone: equal on kills, on tokens, and on vocabulary leaks without them. Census: the Read hook denied every attempt on the package under test; the writer then printed the file through the shell in most runs, the vector the hook leaves open. Words alone do not hold this rule. |
| 3aef3e6c | agents/test-writer.md                  | ## What you do                                              | judgment, removal round     | Acceptance criteria first, then the tests, then run the suite. The criteria sentence stays by judgment: no bench has a criteria file. The plan-heading command, the pointer to the authoring file, and the one-test-per-behavior step are gone: equal on kills, lines within a tenth, and tokens without them. The authoring file itself is deleted, since no other agent loaded it. |
| d8d5bde2 | agents/test-writer.md                  | ## Failing tests are findings, not your bugs                | unproven                    | A red test that follows the plan is reported red, never weakened to go green. The signal the code skill routes a fix on. Carried in both arms of the removal round, which produced no red test, so it has never been exercised. |
| a3534e1a | agents/test-writer.md                  | ## Fences                                                   | removal round               | Never edit source, never dispatch agents, mechanical compile-fixes to existing tests are allowed. Kept in the contract-only text; no run in either arm touched a source file, and the frontmatter also denies the Agent tool. |
| 3998c474 | agents/test-writer.md                  | ## Report (last lines, machine-read)                        | removal round               | The machine-read block the code skill parses: tests written, the run command, red tests, underspecified behaviors, the review pointer, and the trailing red-test line. Kept verbatim; every run in both arms produced it. |
| 9c8ab1a0 | skills/_shared/reviewer-calibration.md | # Reviewer Calibration (single source of truth)             | census                      | Names the four specialist adopters and warns that a renamed section breaks them. The warning failed once, which is why the doctor now checks the references. The diff reviewer is no longer an adopter. |
| b4ab6a17 | skills/_shared/reviewer-calibration.md | ## Persistent Memory                                        | unproven                    | Project memory of known patterns. Never exercised: every review round dispatches without a memory directory. The diff reviewer carries its own copy of this paragraph. |
| 0d9c6962 | skills/_shared/reviewer-calibration.md | ## Calibration Anchor                                       | census                      | The merge-blocking question and the examples that set the bar's height. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it: two replicates at opus found it inert on recall and on false findings for that reviewer. |
| e79a8d75 | skills/_shared/reviewer-calibration.md | ## Verify the Premise Before Flagging                       | judgment                    | Check the code, the rule, and the diff before a finding ships. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it, for the same reason as the block above. |
| a20c02f1 | skills/_shared/reviewer-calibration.md | ## Disposition                                              | census                      | One disposition per finding, the blocker flag, and the plan-impact routing. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it and carries the plan-impact prefix in its own report template instead. |
| 5b08c8b2 | skills/_shared/reviewer-calibration.md | ## Self-Check Before Reporting                              | judgment                    | The calibration question run once more per finding. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it, for the same reason as the anchor block. |
| ea32292f | skills/_shared/code-vocabulary.md      | # Code Vocabulary (what may never appear in committed code) | judgment                    | The boundary between the private operator workflow and the shipped tree. Referenced by the criteria author and the code skill, and named in the messages of the two hooks that enforce it. No coder, the diff reviewer, and the test writer load it: two replicates at opus found the reviewer without it equal on recall and on false findings. Eight Sonnet runs per arm found the writer equal on kills and leak-free with a one-sentence rule in its own prompt. Eight Sonnet runs per arm found the coder equal on every test with the same one-sentence rule, where no run under the full core followed its pointer to this file and that arm leaked the one marker. |
| e616ade4 | skills/_shared/code-vocabulary.md      | ## The rule                                                 | judgment                    | Bans pipeline vocabulary (phase numbers, decision ids, plan paths, process narration) from committed code.                                                                           |
| f9f922c6 | skills/_shared/code-vocabulary.md      | ## What to write instead                                    | judgment                    | A comment says what the code cannot say, not where the decision was made.                                                                                                            |
| 32b8f59e | skills/_shared/code-vocabulary.md      | ## Sweep before handing back                                | judgment                    | Diff-check against the banned table before reporting. Binds the criteria author and the code skill's criteria step; the hooks point here in their messages. The coders, the diff reviewer, and the test writer no longer load this file. |
| 226339be | skills/coder-core/SKILL.md | (preamble) | removal round | Frontmatter: the name the coder and the deep coder preload by, and the description. Byte-identical in both arms of the removal round. |
| e84505b9 | skills/coder-core/SKILL.md | # Coder Core Directives | removal round | The role line: implement the plan, decide no architecture, report and stop on an open design question. Kept in the contract-only text that matched the full core on every held-out and downstream test in eight Sonnet runs per arm at 18% fewer tokens. The sentence about the agent file's scope fence is gone with the fences it described. |
| c72214d7 | skills/coder-core/SKILL.md | ## You are the terminal implementer (HARD RULE) | removal round, judgment | Never dispatch, never spawn a reviewer; the REVIEW line is the only review signal; screenshots to /tmp. Kept in the contract-only text that matched the full core. Not itself varied. The screenshot line moved here from the dropped style block by judgment. |
| aa5ceb79 | skills/coder-core/SKILL.md | ## Read the plan phase-scoped | removal round | Read the shared sections, your phase, and the testing strategy; a sibling's internals are a PLAN-IMPACT. Kept as the rule only. The rg command and the three-Reads mechanics are gone: without them the coder opened the plan half as often at the same test passes, which is where most of the token saving came from. |
| 58286715 | skills/coder-core/SKILL.md | ## Tests are not yours (HARD RULE — coder/test-writer split) | removal round, judgment | The coder/test-writer split, compile-fix and red-test reporting, acceptance criteria as spec, and the one-sentence vocabulary ban. Kept in the contract-only text that matched the full core: no test write in either arm. The one-sentence ban leaked nothing where the full core's pointer to the vocabulary file, never opened in eight runs, leaked one decision id. The criteria sentence stays by judgment: no bench has a criteria file. |
| ceb026d5 | skills/coder-core/SKILL.md | ## Verify before you report | removal round | One sentence: run the project's quality checks. What remains of the pre-submission checklist; equal on every test with the checklist gone. The one-re-run limit lives in the quality-check-cap hook. |
| 547df757 | skills/coder-core/SKILL.md | ## When to stop and ask | unproven | The four stop conditions, with the Phase 0 freeze. Carried in both arms of the removal round; no run hit one, so it is unexercised. |
| 92bc92b0 | skills/coder-core/SKILL.md | ## PLAN-IMPACT findings (structured, never prose) | unproven | The block shape the code skill parses. Carried in both arms of the removal round; no run raised one. |
| c9056e80 | skills/coder-core/SKILL.md | ## Review handoff (last lines of your report) | removal round | The BEHAVIOR, WHY, REFACTOR CANDIDATES, REVIEW, and PLAN-IMPACT: yes lines the review loop parses. Kept in the contract-only text; every line present in all runs under both wordings. The calibration prose around them is gone: equal on presence and on non-none WHY lines. |
| 4caf2a32 | agents/coder.md | (preamble) | removal round, unproven | Dispatch contract: name, description, the sonnet pin, no Agent tool, the core-skill preload, and the both-ends-of-a-wire line. Byte-identical in both arms of the removal round, so not itself varied. The wire line had no target on a single-package phase. |
