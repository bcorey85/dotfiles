-- alabaster (p00f's port of tonsky's theme): minimal-highlight philosophy —
-- color ONLY what carries meaning (strings, constants, definitions, comments),
-- everything else body fg. One scheme, branches on vim.o.background.
return { "p00f/alabaster.nvim", lazy = true }
