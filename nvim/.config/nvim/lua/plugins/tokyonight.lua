-- tokyonight — folke's blue-violet night palette. Four variants registered as
-- separate schemes: storm/night/moon (dark) and day (light).
-- Loads via the theme switcher (theme-mode use tokyonight).
return {
  "folke/tokyonight.nvim",
  lazy = true,
  priority = 1000,
}
