# Rule anchors

Every heading block of the prompt files listed below has a content hash and the
reason it exists. `claude-doctor` recomputes the hashes and reports two faults: a
block with no row, and a row whose block no longer exists. Whitespace does not move
a hash.

The prompt files never carry this provenance. It lives here, and the change ledger
(`agent-evals.md`) holds the full story behind each evidence kind.

To change an anchored block:

1. Edit the prompt file.
2. Run `~/.claude/scripts/rule-anchors.sh list <file>` and copy the new hash.
3. Replace the row's hash. Set `evidence` and `reason` to what backs the new text.
4. Run `claude-doctor`. Git history keeps the old rows.

Evidence kinds: `gate ranking`, `census`, `removal round`, `seeded round`,
`controlled replay`, `judgment` (nobody measured it), `unproven` (never exercised).

| hash     | file                                   | section                                                     | evidence                    | reason                                                                                                                                                                               |
| -------- | -------------------------------------- | ----------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 6b04ae9b | agents/code-reviewer.md                | (preamble)                                                  | seeded round, removal round | Dispatch contract: name, description, tools, project memory, and the one-line role. The opus pin stays because sonnet at this gate missed planted defects that the same prompt caught on opus. The role line's restraint clause went with the judgment text: two replicates at opus found the reviewer without it equal on recall and on false findings. |
| b4ab6a17 | agents/code-reviewer.md                | ## Persistent Memory                                        | unproven                    | Project memory of known patterns, copied from the shared calibration file when the diff reviewer stopped loading that file. Never exercised: every review round dispatches without a memory directory. |
| fbc646b2 | agents/code-reviewer.md                | ## Specialist Scope                                         | removal round               | The four specialist-ownership bullets, kept verbatim in the contract-only text that matched the full prompt in two replicates at opus. Not themselves varied. The suppression list they sat in is gone: inert on recall and on false findings. |
| 9411a7fc | agents/code-reviewer.md                | ### Step 1: Determine Scope                                 | removal round               | How the reviewer finds the diff: the handoff block first, else the git working state. Kept verbatim in the contract-only text that matched the full prompt in two replicates at opus. Not itself varied. |
| 8aac1ba9 | agents/code-reviewer.md                | ### Step 2: Read the Changes                                | removal round               | Read every file in scope and the project conventions file. The three required audits and the two guard sentences that followed them are gone: two replicates at opus found the reviewer without them equal on recall and on false findings, at about seven tenths of the tokens. |
| 8b91a12b | agents/code-reviewer.md                | ## Output Format                                            | removal round, judgment     | The report template the dispatcher parses: summary headings, prior issues, Fix/Ask/Nit. Kept verbatim in the contract-only text that matched the full prompt in two replicates at opus, plus one sentence added by judgment: the plan-impact prefix that the review loop scans for. That prefix used to live in the removed disposition text, and no bench exercises it. |
| 1abfce31 | agents/code-reviewer.md                | ## Reviewer-Specific Tool Use                               | removal round               | What remains after the removal: trust the handoff's checks, and stay in scope with two standing exceptions. Kept in the contract-only text that matched the full prompt in two replicates at opus. |
| 31c1975f | agents/test-writer.md                  | (preamble)                                                  | removal round               | Dispatch contract: name, description, the sonnet pin, no Agent tool, and the two-line role: one phase, after the implementation, without reading it. Kept verbatim in the contract-only text that matched the full prompt on planted-bug kills and on tokens in eight Sonnet runs per arm. The sonnet pin is untested. |
| 42459b9d | agents/test-writer.md                  | ## Implementation blindness (HARD RULE — the reason you exist) | removal round, census    | The MAY and MUST NOT lists, the plan-gap rule, and the one-sentence vocabulary rule. Kept in the contract-only text that matched the full prompt. The "stop scrolling" sentence and the pointer to the vocabulary file are gone: equal on kills, on tokens, and on vocabulary leaks without them. Census: the Read hook denied every attempt on the package under test; the writer then printed the file through the shell in most runs, the vector the hook leaves open. Words alone do not hold this rule. |
| 3aef3e6c | agents/test-writer.md                  | ## What you do                                              | judgment, removal round     | Acceptance criteria first, then the tests, then run the suite. The criteria sentence stays by judgment: no bench has a criteria file. The plan-heading command, the pointer to the authoring file, and the one-test-per-behavior step are gone: equal on kills, lines within a tenth, and tokens without them. The authoring file itself is deleted, since no other agent loaded it. |
| d8d5bde2 | agents/test-writer.md                  | ## Failing tests are findings, not your bugs                | unproven                    | A red test that follows the plan is reported red, never weakened to go green. The signal the code skill routes a fix on. Carried in both arms of the removal round, which produced no red test, so it has never been exercised. |
| a3534e1a | agents/test-writer.md                  | ## Fences                                                   | removal round               | Never edit source, never dispatch agents, mechanical compile-fixes to existing tests are allowed. Kept in the contract-only text; no run in either arm touched a source file, and the frontmatter also denies the Agent tool. |
| 3998c474 | agents/test-writer.md                  | ## Report (last lines, machine-read)                        | removal round               | The machine-read block the code skill parses: tests written, the run command, red tests, underspecified behaviors, the review pointer, and the trailing red-test line. Kept verbatim; every run in both arms produced it. |
| cf82dff3 | agents/test-intent-reviewer.md         | (preamble)                                                  | removal round               | Dispatch contract: name, description, the opus pin, the read-only tool list, the boxed question, and the one-sentence definition of a pinned bug with the read-the-implementation-for-what-not-whether rule folded in. The separate "core rule" section, the one-sided-pin paragraph, and the six snapshot smells are gone: the contract-only text found every planted bug-pin and cull in every run that the full text did, with no new false positives. |
| 59d80080 | agents/test-intent-reviewer.md         | ## Step 1 — Resolve the intent oracle                       | removal round, judgment     | The resolver call, the oracle priority (ticket, then success criteria, plan mechanics demoted), the three exit codes, and the oracle-strength tag. Kept verbatim; every run in both arms resolved the oracle and tagged findings spec-backed. The ticket filename is `00-ticket.md`, the resolver's real name, by judgment. |
| 47d51527 | agents/test-intent-reviewer.md         | ## Step 2 — Scope to changed tests only                     | removal round               | Changed files only, and the two dispatch halves with the run-both fallback. Kept verbatim; no run in either arm reported findings from the half it was not sent. |
| 766d72d7 | agents/test-intent-reviewer.md         | ## Step 3 — Audit each assertion against the oracle         | removal round               | The three labels: pins intent, pins bug, unverifiable. The list of cull shapes and the "diff noise" sentence that followed are gone: equal on every planted bug-pin, and the decoy tests were not flagged in either text after audit. |
| 7e8774b9 | agents/test-intent-reviewer.md         | ## Step 4 — Cull check (added tests only)                   | removal round               | The one cull question, the read-only mutation fence with its route to the mutation tester, the one-smoke-test exemption, and the coverage-gap boundary. The named cull shapes are gone: equal on all three planted duplicates. |
| 1d90ed63 | agents/test-intent-reviewer.md         | ## Step 5 — Coverage-net check (deleted tests only)         | removal round               | Deleted and net-removed assertions, search the surviving suite, the two exemptions, and the denominator-first rule. The "coverage often moves" sentence is gone: equal on the planted deleted test. The denominator is counted suite-wide by the full text and package-wide by this one; the sentence is the same in both, so the difference is the reviewer's choice. |
| fbd25ec7 | agents/test-intent-reviewer.md         | ## Step 6 — Weak-assertion sweep (branch-added tests, whole-suite scope) | removal round, seeded round | The weak-test definition, the shape pass, the absence pass with its required `Promises walked: N, unpinned: M` count, the no-citation-means-unverifiable rule, and the test-writer route. The six named weak shapes are gone: all six planted shapes were flagged in every run without them. The required count is what makes the absence pass run: with no count required, a planted plan promise with no test went unfound in every run of both the long and the short text; with the count required it was found in every run, at about a quarter more reviewer tokens. |
| 7f4483a3 | agents/test-intent-reviewer.md         | ## Output Format                                            | removal round, seeded round | The report block the dispatcher relays and the scorer reads, with the base-suite and promises-walked header lines marked cull half only. The boundary section and the guidelines that sat around it are gone: equal on every plant, and no run in either arm wrote to the tree. The promises line is the output the absence pass owes; see Step 6. |
| 428d2b1c | agents/plan-reviewer.md                | (preamble)                                                  | removal round               | Dispatch contract: name, description, the opus pin, the read-only tool list, and the boxed question. The "first reader not in the room" paragraph is gone: equal on every planted defect without it. |
| 7cc479bb | agents/plan-reviewer.md                | ## Your inputs                                              | removal round               | Read the dispatched paths from disk, read the codebase for assumed-exists claims, never the ledger or research unless named. Kept verbatim; every run in both arms read the plan, the ticket, and the tree. |
| ef761f06 | agents/plan-reviewer.md                | ## What you look for                                        | removal round               | One paragraph per finding class, re-entry, and the enumerate-never-sample rule. The bullets under tree claims (citation decay, batching), the rubber-duck prose, and the settled-decisions line are gone: one plant per class was found in every run without them, and the DUCK pass still ran from the output block alone. Without the bullets the reviewer stayed in the tree instead of scanning the live corpus the plan cited. |
| 96e8c7ba | agents/plan-reviewer.md                | ## The bar for an alternative                               | removal round               | One sentence: what changes, what disappears, what it costs, why the authors missed it, and zero ALT is correct. The four bullets, the "Consider using X" sentence, and the second zero-ALT copy are gone: no in-tree alternative was lost. The contract text wrote no ALT where the full text wrote corpus-driven ones; whether it suppresses in-tree alternatives is untested. |
| 4659a9be | agents/plan-reviewer.md                | ## The bar for a mechanism you propose                      | removal round               | The `Evidence-class:` line, exercised or declared-only, never omitted. Kept verbatim. |
| 5b516465 | agents/plan-reviewer.md                | ## What is not yours                                        | removal round               | Argue, never redesign; no code quality, security, or performance. Kept verbatim; no run in either arm wrote to the tree or reviewed code. |
| 2ebb29be | agents/plan-reviewer.md                | ## Output                                                   | removal round               | The DUCK-first report block, the four severity definitions, and the VERDICT line. The closing "invented-finding pass is worse" paragraph is gone: zero invented findings on the unmodified plan in either arm without it. |
| e887ef3b | agents/test-reviewer.md                | (preamble)                                                  | removal round               | Dispatch contract: name, description, the opus pin, the read-only tool list, and the role line with the mission folded in. The Primary Mission heading is gone: equal on every planted gap, weak test, and cull candidate without it. |
| b6b6650d | agents/test-reviewer.md                | ## Calibration — restraint over thoroughness                | removal round               | The schedule-work test, never demote a gap to nit, the hedging rule, and the silent-regression bar. Kept verbatim; the six closing guidelines that restated it are gone with no change in findings or decoy flags. |
| 1c603dc4 | agents/test-reviewer.md                | ## Target Scope                                             | removal round               | The four scopes and the branch diff command. Kept verbatim; every branch-scope run in both arms ran the diff and reviewed exactly the four files it listed. |
| 20fb8635 | agents/test-reviewer.md                | ## Review Process                                           | removal round               | One paragraph per step: map, gaps with the must-test rule, the weak/brittle/stale/missing classes, hygiene, and the cull rule with its exemptions. The pattern bullets and the four cull shapes are gone: every plant was found in every run without them, and the reviewer named its own shapes for each cull candidate. |
| d79bbe86 | agents/test-reviewer.md                | ## Output Format                                            | removal round               | The scope header, the health grade, the seven finding sections, and the summary. Kept verbatim; every run in both arms produced it, and the section a plant landed in was the same across arms. |
| 9c8ab1a0 | skills/_shared/reviewer-calibration.md | # Reviewer Calibration (single source of truth)             | census                      | Names the four specialist adopters and warns that a renamed section breaks them. The warning failed once, which is why the doctor now checks the references. The diff reviewer is no longer an adopter. |
| b4ab6a17 | skills/_shared/reviewer-calibration.md | ## Persistent Memory                                        | unproven                    | Project memory of known patterns. Never exercised: every review round dispatches without a memory directory. The diff reviewer carries its own copy of this paragraph. |
| 0d9c6962 | skills/_shared/reviewer-calibration.md | ## Calibration Anchor                                       | census                      | The merge-blocking question and the examples that set the bar's height. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it: two replicates at opus found it inert on recall and on false findings for that reviewer. |
| e79a8d75 | skills/_shared/reviewer-calibration.md | ## Verify the Premise Before Flagging                       | judgment                    | Check the code, the rule, and the diff before a finding ships. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it, for the same reason as the block above. |
| a20c02f1 | skills/_shared/reviewer-calibration.md | ## Disposition                                              | census                      | One disposition per finding, the blocker flag, and the plan-impact routing. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it and carries the plan-impact prefix in its own report template instead. |
| 5b08c8b2 | skills/_shared/reviewer-calibration.md | ## Self-Check Before Reporting                              | judgment                    | The calibration question run once more per finding. Adopted by the four specialist reviewers, untested there. The diff reviewer no longer loads it, for the same reason as the anchor block. |
| ea32292f | skills/_shared/code-vocabulary.md      | # Code Vocabulary (what may never appear in committed code) | judgment                    | The boundary between the private operator workflow and the shipped tree. Referenced by the criteria author and the code skill, and named in the messages of the two hooks that enforce it. No coder, the diff reviewer, and the test writer load it: two replicates at opus found the reviewer without it equal on recall and on false findings. Eight Sonnet runs per arm found the writer equal on kills and leak-free with a one-sentence rule in its own prompt. Eight Sonnet runs per arm found the coder equal on every test with the same one-sentence rule, where no run under the full core followed its pointer to this file and that arm leaked the one marker. |
| e616ade4 | skills/_shared/code-vocabulary.md      | ## The rule                                                 | judgment                    | Bans pipeline vocabulary (phase numbers, decision ids, plan paths, process narration) from committed code.                                                                           |
| f9f922c6 | skills/_shared/code-vocabulary.md      | ## What to write instead                                    | judgment                    | A comment says what the code cannot say, not where the decision was made.                                                                                                            |
| 32b8f59e | skills/_shared/code-vocabulary.md      | ## Sweep before handing back                                | judgment                    | Diff-check against the banned table before reporting. Binds the criteria author and the code skill's criteria step; the hooks point here in their messages. The coders, the diff reviewer, and the test writer no longer load this file. |
| 226339be | skills/coder-core/SKILL.md | (preamble) | removal round | Frontmatter: the name the coder and the deep coder preload by, and the description. Byte-identical in both arms of the removal round. |
| e84505b9 | skills/coder-core/SKILL.md | # Coder Core Directives | removal round | The role line: implement the plan, decide no architecture, report and stop on an open design question. Kept in the contract-only text that matched the full core on every held-out and downstream test in eight Sonnet runs per arm at 18% fewer tokens. The sentence about the agent file's scope fence is gone with the fences it described. |
| c72214d7 | skills/coder-core/SKILL.md | ## You are the terminal implementer (HARD RULE) | removal round, judgment | Never dispatch, never spawn a reviewer; the REVIEW line is the only review signal; screenshots to /tmp. Kept in the contract-only text that matched the full core. Not itself varied. The screenshot line moved here from the dropped style block by judgment. |
| aa5ceb79 | skills/coder-core/SKILL.md | ## Read the plan phase-scoped | removal round | Read the shared sections, your phase, and the testing strategy; a sibling's internals are a PLAN-IMPACT. Kept as the rule only. The rg command and the three-Reads mechanics are gone: without them the coder opened the plan half as often at the same test passes, which is where most of the token saving came from. |
| 58286715 | skills/coder-core/SKILL.md | ## Tests are not yours (HARD RULE — coder/test-writer split) | removal round, judgment | The coder/test-writer split, compile-fix and red-test reporting, acceptance criteria as spec, and the one-sentence vocabulary ban. Kept in the contract-only text that matched the full core: no test write in either arm. The one-sentence ban leaked nothing where the full core's pointer to the vocabulary file, never opened in eight runs, leaked one decision id. The criteria sentence stays by judgment: no bench has a criteria file. |
| ceb026d5 | skills/coder-core/SKILL.md | ## Verify before you report | removal round | One sentence: run the project's quality checks. What remains of the pre-submission checklist; equal on every test with the checklist gone. The one-re-run limit lives in the quality-check-cap hook. |
| 547df757 | skills/coder-core/SKILL.md | ## When to stop and ask | unproven | The four stop conditions, with the Phase 0 freeze. Carried in both arms of the removal round; no run hit one, so it is unexercised. |
| 92bc92b0 | skills/coder-core/SKILL.md | ## PLAN-IMPACT findings (structured, never prose) | unproven | The block shape the code skill parses. Carried in both arms of the removal round; no run raised one. |
| c9056e80 | skills/coder-core/SKILL.md | ## Review handoff (last lines of your report) | removal round | The BEHAVIOR, WHY, REFACTOR CANDIDATES, REVIEW, and PLAN-IMPACT: yes lines the review loop parses. Kept in the contract-only text; every line present in all runs under both wordings. The calibration prose around them is gone: equal on presence and on non-none WHY lines. |
| 4caf2a32 | agents/coder.md | (preamble) | removal round, unproven | Dispatch contract: name, description, the sonnet pin, no Agent tool, the core-skill preload, and the both-ends-of-a-wire line. Byte-identical in both arms of the removal round, so not itself varied. The wire line had no target on a single-package phase. |
| 73f3f256 | agents/review-loop.md | (preamble) | removal round | Dispatch contract: name, description, the tool list with Agent and no write tool, project memory, and no model line, since the callers pass sonnet. Byte-identical in both arms of the removal round, so not itself varied. |
| 26613c96 | agents/review-loop.md | # Review Loop | removal round | The role: own the review-fix loop in an isolated context and return one packet, with no modal tool. Byte-identical in both arms of the removal round, so not itself varied. |
| f76b2191 | agents/review-loop.md | ## Inputs (from the dispatch prompt) | removal round | The dispatch arguments: mode, caller, lane, the handoff block, the deep and specialist flags, the reviewers list, and no-review. The trimmed arm dropped only the sentence on why lane must be set. That arm failed the packet and token checks, so the whole text holds. |
| 17f5f453 | agents/review-loop.md | ## Step 0: Log the invocation (always, first action) | removal round | Append the skill-usage line by hand, because the logging hook sees Skill calls and not agents. The trimmed arm kept the command and dropped the reason. Both arms logged the line in 12 of 12 runs, so the reason is unproven on its own. It holds with the rest. |
| 40bbdafa | agents/review-loop.md | ## Loop ordering (GATE-CRITICAL — do not reorder) | removal round | The iteration order as pseudo-code, and the paragraph on how the commit gate marks dirty and why only the caller marks clean. The trimmed arm dropped the paragraph. No run in either arm ran the clean mark, so the paragraph is unproven on its own. It holds with the rest. |
| 60e11150 | agents/review-loop.md | ## Step 1: Parse args | removal round | The two iteration counters and their budgets, the global backstop, and the handoff block as scope. The trimmed arm dropped one aside on the specialist budget, and failed the packet and token checks. |
| ce590ba4 | agents/review-loop.md | ## Step 2: Determine review scope | removal round | How the loop finds the diff: the handoff files, else the git working state, plus the second-order supplement. The trimmed arm dropped one sentence that forbids a second review of unchanged code, and failed the packet and token checks. |
| cd9fe78d | agents/review-loop.md | ## Step 3: Dispatch code-reviewer subagent(s) | removal round | Reviewer continuity on re-entry, the split threshold, the split boundary, and what each reviewer is passed. The trimmed arm dropped three clauses on scope. Every run in both arms dispatched one or two reviewers on the same threshold. |
| dcba08a3 | agents/review-loop.md | ## Step 4: Classify the reviewer output | removal round | Route each finding on the label the reviewer gave it, never re-label, and the plan-impact and perf rules. The trimmed arm dropped the plan-impact definition and stopped for a plan decision in one run where the full text stopped in five. It sent a coder after the same judgment calls at 20% more loop tokens at the median. The definition is the likely routing text, untested alone. |
| b8eedbfe | agents/review-loop.md | ## Step 5: Fix dispatch (`fix` findings only) | removal round | One coder with the fix findings only, coder continuity, the verbatim fence, and the coder-report post-processing. The trimmed arm dropped three asides. Coder dispatches were 9 against 8 over twelve runs per arm, with no fix outside a coder in either arm. |
| 32f21a3d | agents/review-loop.md | ## Step 5b: `mode: fix-first` | removal round, unproven | The fix-first mode: sources of findings in priority order, one coder, the test guard, and the no-review return. Carried in both arms with two sentences dropped. No run used fix-first, so it is unexercised. |
| f1130e41 | agents/review-loop.md | ## Step 5c: Deferral — the one-round budget | removal round | The one-round budget: on re-entry, fix blockers now and defer the rest to the branch-exit queue with the branch field. The trimmed arm dropped the rationale and deferred in three runs where the full text deferred in none. Deferred rows were logged in both arms. |
| d67c01d1 | agents/review-loop.md | ## Step 5d: Fix-diff verification (once per loop, whenever a fix coder ran) | removal round | One reviewer pass over the fix diff before any exit, once per loop, outside the caps. The trimmed arm dropped two rationale sentences. The pass followed every coder in the full arm, 5 of 5, and 5 of 6 in the trimmed arm, which once returned deferred with the fix diff unread. |
| 07355816 | agents/review-loop.md | ## Step 6: Convergence — the execution gate | removal round | The execution gate, the acceptance-spec exception, no test-intent audit here, and the class-closure check before convergence. The trimmed arm dropped the rationale and one example. Typecheck was clean in 24 of 24, no run dispatched test-intent, and no run converged empty past round one in either arm. |
| 4dbef8b8 | agents/review-loop.md | ## Step 6b: Cross-cutting specialist pass (post-convergence, deterministic trigger) | removal round | The post-convergence specialist pass: skip conditions, eligibility from three signals, dispatch, fold, and the record line. The trimmed arm dropped the floor-not-ceiling and absence-is-not-clearance sentences. Security ran on the same tree in both arms from its path trigger. Smell ran in two full runs after three coders grew the diff past the size trigger, and in no trimmed run. |
| 532483d7 | agents/review-loop.md | ## Step 7: Log the run (every invocation — the loop's flywheel) | removal round | The metrics line by absolute path, and the field glossary. The trimmed arm dropped the reasons. Consistent rows were 9 against 8, with the misses in the plan-impact and deferred exits of both arms. |
| 39e550f4 | agents/review-loop.md | ### Step 7b: Per-finding rows | removal round | Per-gate and per-finding rows per the shared finding-log file, after the specialist pass. Byte-identical in both arms. Run rows were logged in 11 against 12 runs, finding rows 61 against 68. |
| 9dc91615 | agents/review-loop.md | ## Return packet (the ONLY thing the orchestrator pays for) | removal round | The packet template the callers parse, with its status-conditional fields. Byte-identical in both arms. The trimmed arm omitted empty conditional fields in 10 of 12 packets against 3 of 12, with the rationale around it gone. No caller reads those fields under those statuses. |
| d5e3fb4f | agents/review-loop.md | ## What NOT to do | removal round | No modal, no write around the missing tools, never the clean mark, no narration, no empty converged past round one. The trimmed arm dropped three asides. No shell write, gate mark, or test-intent dispatch in either arm, and narration lines were equal at one per run. |
| ca4cab53 | skills/code/SKILL.md | (preamble) | removal round | Invocation contract: name, description, and the tool list. Byte-identical in both arms of the removal round, so not itself varied. |
| 904e5976 | skills/code/SKILL.md | # Code | removal round | The one-line role and the argument line. Byte-identical in both arms. |
| 4b2c879c | skills/code/SKILL.md | ## Modifiers | removal round | The pointer to the shared modifiers file, read only when a modifier is present. |
| 2f3b04dc | skills/code/SKILL.md | ## Instructions | removal round | The routing rules only: resolve the task input, read the plan phase-scoped, check criteria, dispatch one coder then the test-writer, raise plan impact, dispatch the review loop with the handoff, route on its status, flip the phase line, and re-enter at the phase boundary. The rationale, the restated schemas, and the negatives of the earlier text are gone; two removal rounds showed the orchestrator dispatches the same agents in the same order with the same parameters and stops in the same places without them, at about half the loaded words. Two rules had to move rather than go: the clean gate mark is the first sentence of the converged branch, not the last (skipped in 3 of 3 runs at the tail, run in 3 of 3 at the head), and the cap branch keeps the sentence that the dirty session is what keeps the commit blocked (without it the capped phase was marked done in 3 of 3 runs, with it in 1 of 3, the same as the earlier text). |
| e99542e7 | skills/code/SKILL.md | ## Phase-Complete Block | removal round | Blocks A, B, and C, the brief pointer, the verification-item rule, and the stage-queue walkthrough. The "what must EXIST" intro, the placeholder notes, the classifier description, and the walkthrough fences are gone; sign-off text, the manual item, the auto-advance notice, and the stage invocation held at the earlier text's rate without them. |
| 0a133228 | skills/code/SKILL.md | ## Task | removal round | The argument placeholder. Byte-identical in both arms. |
| 20a3709f | skills/review/SKILL.md | (preamble) | removal round | Frontmatter: name, description, and the allowed tools. Byte-identical in both arms. |
| c369d228 | skills/review/SKILL.md | # Code Review | removal round | The one-line role: dispatch the loop, render its packet, raise the modals it cannot. The "its instructions never enter this context" aside is gone: the wrapper dispatched the same agent with the same parameters in every run without it. |
| db2fbd3c | skills/review/SKILL.md | ## Modifiers | removal round | The modifier and specialist-flag pass-through lines. The "agent is unpinned" aside is gone: every run passed `model: sonnet` without it. |
| 2bf9c846 | skills/review/SKILL.md | ## Instructions | removal round | The dispatch step with its five pass-through items, the five status routes with every STOP and do-NOT rule, the seven render lines in order, the ask step with the log command, and the nothing-outstanding line. The rationale clauses (empty fixed list is a bug, stopping-rule receipt, auto-fixing an ask is most wrong, the ask-outcome yield sentence, the normal-completion and branch-recap notes, gate dirty by design) are gone: the wrapper routed every canned status, rendered every line, marked the gate only on converged or deferred, and never auto-fixed an ask in every run without them, at the same tokens. The cap-reached route keeps the sentence that the dirty session keeps the commit blocked, because the code skill's round found the wrapper marks a capped phase done without it. The missing-receipt sentence under class_closure is kept but does not work in either wording: with no class_closure line in a converged packet the wrapper wrote `none` and marked the gate clean in most runs of both arms. |
| 1943f83e | skills/review/SKILL.md | ## Plan-impact findings (unskippable routing) | removal round | The four steps: never fold it in, raise the modal with the three options before any dispatch, record the answer in the plan's deviations section, re-dispatch with both counters. The "modal blocks until answered" and the verify/adr cross-references are gone: every run surfaced the plan-impact packet with its three options and dispatched nothing else without them. |
| 04cb01da | skills/review/SKILL.md | ## Arguments | removal round | The argument placeholder. Byte-identical in both arms. |
| ee753845 | agents/smell-reviewer.md | (preamble) | removal round | Frontmatter: name, description, model, tools, memory, color. Identical in both arms apart from the model pin the round set to measure at the loop's tier. |
| c4acb849 | agents/smell-reviewer.md | ## Inherit the calibration verbatim | removal round | The calibration read with its five named sections. Byte-identical in both arms. |
| 9fcdcacc | agents/smell-reviewer.md | ## Your scope — ONLY these, and ONLY inside your dispatched bound | removal round | The five scope items with the prior-art search and the shared-decision rule, the defensive-scaffolding test, the dead-export reference search, disposition by consequence, the anti-churn line, and the bound paragraph. The rationale sentences (the author's blind spot, size does not excuse it, defensive scaffolding is the agentic form, the `export` keyword hides a death, premature abstraction, structure does not stop a phase, a candidate you can't name) are gone: on a seeded diff with one plant per scope item, the reviewer found all six in every run with the same dispositions and the same cited searches without them, and filed no invented finding on the clean twin. |
| 92a2412b | agents/smell-reviewer.md | ## Format (required) | removal round | The `[smell]` prefix, the `[design-decision]` routing tag, and its threshold sentence. The "blast radius is a fact to state" and "look for the shape that preserves every guarantee" sentences are gone: no run in either arm raised a design-decision tag on either tree. |
| 0209cf83 | agents/smell-reviewer.md | ## Explicitly NOT your scope | removal round | The four deferrals and the single closing `Note:` rule. Byte-identical in both arms; every out-of-domain observation landed as a closing note in both. |
| 2ae45350 | agents/smell-reviewer.md | ## Process | removal round | The four steps: scope, prior-art pass, dead-reference pass, project CLAUDE.md. Byte-identical in both arms. |
| aa3a7d1b | agents/smell-reviewer.md | ## Output Format | removal round | The summary block with Fix, Ask, and Nit. Byte-identical in both arms. |
