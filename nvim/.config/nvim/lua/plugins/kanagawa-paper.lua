-- kanagawa-paper (thesimonho): a softer, dustier take on kanagawa — ink (dark)
-- paired with canvas (warm paper light). The "kanagawa-paper" theme FAMILY;
-- scheme mapping, accents and readability fixups live in theme-sync's FAMILIES
-- table. Switch with `theme-mode use kanagawa-paper`. lazy auto-loads the
-- colorscheme on demand, so this stays benched at startup.
return { "thesimonho/kanagawa-paper.nvim", lazy = true }
