*.log
*.local.yml
nvim/.config/nvim/lazy-lock.json

# theme-mode template contract: the stowed *.template is tracked, the generated
# config next to it is machine-local. Stow folds these single-file dirs into a
# directory symlink, so the generated file lands inside the working tree.
tuicr/.config/tuicr/config.toml
herdr/.config/herdr/config.toml
starship/.config/starship.toml

# Machine-specific hyprland config (created by install/hyprland)
hypr/.config/hypr/hyprland.local.conf
hypr/.config/hypr/machine.lua

# === Claude Code (claude/.claude/) ===
# Allowlist strategy: ignore everything by default, then un-ignore the
# authored config we want shared. New runtime files Claude drops in here
# are ignored automatically — no need to chase them with new rules.
claude/.claude/*

# Authored config shared across machines:
!claude/.claude/agents/
!claude/.claude/skills/
!claude/.claude/hooks/
!claude/.claude/rules/
!claude/.claude/scripts/
!claude/.claude/CLAUDE.md
!claude/.claude/MCP_LOOP_README.md
!claude/.claude/settings.json
!claude/.claude/keybindings.json
!claude/.claude/statusline-command.sh
!claude/.claude/policy-limits.json
!claude/.claude/.security-level

nvim/.config/nvim/lazy-lock.json

# Claude Code harness-managed runtime state (auto-refreshed by the CLI)
claude/.claude/policy-limits.json

# === opencode (opencode/.config/opencode/) ===
# opencode regenerates these at runtime via the plugin SDK; don't track them.
opencode/.config/opencode/node_modules
opencode/.config/opencode/package.json
opencode/.config/opencode/package-lock.json
opencode/.config/opencode/bun.lock
