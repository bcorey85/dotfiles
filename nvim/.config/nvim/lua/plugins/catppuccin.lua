-- catppuccin — pastel, four flavours. mocha (dark) / latte (light) here;
-- frappe and macchiato are the mid-dark rungs we don't use.
-- Loads via the theme switcher (theme-mode use catppuccin).
return {
  "catppuccin/nvim",
  name = "catppuccin",
  lazy = true,
  priority = 1000,
}
