-- onedark (navarasu/onedark.nvim) — theme-mode family. Dark #282c34 / light
-- #fafafa. Loads eagerly (lazy = false, priority = 1000) so it's on the rtp
-- before config.lazy.lua's theme-sync.start().
return {
  "navarasu/onedark.nvim",
  name = "onedark",
  lazy = false,
  priority = 1000,
}
