---
paths:
  - "**/*.vue"
---

# Vue

- Use `<script setup>` with Composition API for all Vue SFCs.
- Prefer `computed` over methods for derived state.
- Use `defineProps` / `defineEmits` with type-based declaration.
