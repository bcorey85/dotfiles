# Global gitignore — git reads ~/.config/git/ignore by default (gitignore(5))
# when core.excludesFile is unset, so this needs no .gitconfig wiring.
# For machine-and-tool-local files that shouldn't live in every repo's
# .gitignore.

# machine-local Claude Code settings
**/.claude/settings.local.json
