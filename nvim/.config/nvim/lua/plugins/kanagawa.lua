-- kanagawa (rebelot): dragon dark — the fully desaturated ink-wash variant on
-- a warm black — paired with lotus light, the yellow-paper mode. Loaded by
-- theme-sync via the FAMILIES registry; fixups live there too.
return { "rebelot/kanagawa.nvim", lazy = true }
