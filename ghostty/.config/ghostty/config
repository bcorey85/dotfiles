# Font — JetBrains Mono, chosen after an audition for its tall x-height and
# sturdy weight (far less eye-strain than CommitMono for all-day reading).
font-family = JetBrainsMono Nerd Font Mono
font-style = SemiBold
font-size = 11

# Linux/FreeType only: disable light hinting (on by default in 1.3+) so glyph
# stems render heavier — closest match to kitty's bolder text rendering, since
# font-thicken is macOS-only. If still too light, use `font-style = Bold`.
freetype-load-flags = no-light

# Theme - doom-one (Atom One Dark lineage)
background = #282c34
foreground = #bbc2cf

selection-foreground = #bbc2cf
selection-background = #3f444a

cursor-color = #bbc2cf
cursor-text = #282c34

# Color palette — doom-one's canonical ANSI mapping (matches doom-one.nvim's
# terminal_colors), so tmux/nvim/shell read identically.
palette = 0=#1c1f24
palette = 1=#ff6c6b
palette = 2=#98be65
palette = 3=#ECBE7B
palette = 4=#51afef
palette = 5=#c678dd
palette = 6=#46D9FF
palette = 7=#bbc2cf
palette = 8=#5B6268
palette = 9=#ff6c6b
palette = 10=#98be65
palette = 11=#ECBE7B
palette = 12=#51afef
palette = 13=#c678dd
palette = 14=#46D9FF
palette = 15=#DFDFDF

# Window
window-padding-x = 4
window-padding-y = 4

# Line height — extra cell height reduces line-tracking effort when reading
# dense markdown/diffs all day.
adjust-cell-height = 10

# Shell integration
shell-integration = detect

# Don't prompt to confirm closing a surface/window
confirm-close-surface = false

# macOS cmd key mappings
keybind = super+j=text:\x1bj
keybind = super+k=text:\x1bk
keybind = super+l=text:\x1bl
keybind = super+semicolon=text:\x1b;

# Mouse
link-url = true
