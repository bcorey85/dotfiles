-- kanso.nvim (kanagawa successor, built for calm): near-gray body #c5c9c7
-- on gh-dimmed-like canvas #22262d (mist), pre-muted kanagawa accents, real
-- bg diff washes. The measured halfway point between github dimmed and
-- zenwritten. mist = dark, pearl = light.
-- Benched: loads via the theme switcher (theme-use kanso).
return {
  "webhooked/kanso.nvim",
  lazy = true,
}
