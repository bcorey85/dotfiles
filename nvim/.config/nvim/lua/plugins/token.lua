-- token — ThorstenRhau's zero-config warm palette (orange-tinted off-white fg,
-- muted sage/red gitsigns). One scheme for both modes; picks its palette from
-- vim.o.background. Loads via the theme switcher (theme-mode use token).
return {
  "ThorstenRhau/token",
  lazy = true,
  priority = 1000,
}
